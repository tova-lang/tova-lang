name = "Alice"
base_price = 29.99
tax_rate = 0.08

total = base_price * (1 + tax_rate)
print("{name}'s total: {total}")

var retries = 0
var connected = false

for _ in range(3) {
  retries += 1
  if retries == 2 {
    connected = true
  }
}
print("Connected after {retries} tries: {connected}")
