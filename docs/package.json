{
  "name": "tova-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "build:playground": "cd .. && bun playground/build.js && mkdir -p docs/public/playground-app && cp playground/index.html docs/public/playground-app/index.html",
    "docs:dev": "npm run build:playground && vitepress dev",
    "docs:build": "npm run build:playground && vitepress build",
    "docs:preview": "vitepress preview"
  },
  "devDependencies": {
    "lz-string": "^1.5.0",
    "vitepress": "^1.6.3"
  }
}
